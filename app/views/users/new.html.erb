<h1>Sign Up</h1>
<%= form_with model: @user, local: true do |f| %>
  <%= f.label :uid %>
  <%= f.text_field :uid %>

  <%= f.label :password %>
  <%= f.password_field :password %>
  

  

  <%= f.submit 'Create' %>
<% end %>